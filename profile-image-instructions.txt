<!-- 
    PROFILE IMAGE PLACEHOLDER INSTRUCTIONS
    
    To add a real profile image:
    
    1. Place your image file in this folder (recommended: profile.jpg or profile.png)
    2. Replace the SVG placeholder in index.html with:
    
    <img src="profile.jpg" alt="Prehtagonist" class="profile-img">
    
    3. Add this CSS to styles.css:
    
    .profile-img {
        width: 120px;
        height: 120px;
        border-radius: 50%;
        object-fit: cover;
        border: 2px solid var(--light-gray);
        transition: var(--transition);
    }
    
    .profile-img:hover {
        transform: scale(1.05);
        border-color: var(--accent-blue);
    }
    
    For best results, use a high-quality image (400x400px minimum) 
    with a transparent or professional background.
-->